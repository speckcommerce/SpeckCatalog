
<!-- tabs -->
<ul class="tabs" data-tabs="tabs">
    <li class="this-product"><a href="#this-product">This Product</a></li>
    <li class="parent-choices"><a href="#parent-choices">Parent Choices</a></li>
</ul>
<!-- /tabs -->

<!-- panes -->
<div id="my-tab-content" class="tab-content">
    
    <!--"This Product" Pane -->
    <div id="this-product">

            <!-- tabs -->
            <ul class="tabs" data-tabs="tabs">
                <li class="product"><a href="#product">Product</a></li>
                <li class="options"><a href="#options">Options</a></li>
                <li class="specs"><a href="#specs">Specs</a></li>
                <li class="features"><a href="#features">Features</a></li>
                <li class="documents"><a href="#documents">Documents</a></li>
            </ul> 
            <!-- /tabs -->

            <!-- panes -->
            <div id="my-tab-content" class="tab-content">

                <!-- "This Product > Product" Pane -->
                <div class="tab-pane" id="product">
                    <? if($product->getType() === 'product') : ?>
                        <div class="row">
                            <div class="span5">
                                <?=$this->partial('/catalogmanager/partial/editform.phtml', array(
                                    'form' => $forms['product'], 'product' => $product, 
                                ));?>
                            </div>
                            <div class="span5">
                                <?=$this->partial('/catalogmanager/partial/editform.phtml', array(
                                    'form' => $forms['item'], 'item' => $product->getItem(),
                                ));?>
                            </div>
                            <div class="span6">
                                <? $item = $product->getItem(); ?>  
                                <h4>Manufacturer:</h4>
                                <? if($item->getManufacturer()) :?>
                                    <?=$item->getManufacturer();?>
                                <? else : ?>
                                    <?=$this->partial('catalogmanager/partial/add.phtml', array(
                                        'class' => 'company', 'dropdown' => '1',
                                    ));?>
                                <? endif; ?>
                            </div>
                        </div>
                        <div class="parent">
                            <h4>Item Uoms:</h4>
                            <?=$this->partial('catalogmanager/partial/add.phtml', array('class' => 'item-uom', 'add' => 1));?>
                            <?=$this->render('/catalogmanager/partial/product/item/item-uom.phtml');?>
                            <? if (count($item->getUoms()) > 0) : ?>
                                <? foreach($item->getUoms() as $itemUom) : ?>
                                    <?=$this->partial('/catalogmanager/partial/product/item/item-uom.phtml', array(
                                        'itemUom' => $itemUom, 'forms' => $forms
                                    ));?>
                                <? endforeach; ?>
                            <? endif; ?>
                        </div>  
                    <? else: ?>
                        <?=$this->partial('/catalogmanager/partial/editform.phtml', array('form' => $forms['product']));?>
                    <? endif; ?> 
                </div>
                <!-- /"This Product > Product" Pane -->

                <!-- "This Product > Options" Pane -->
                <div class="tab-pane parent" id="options">
                    <?=$this->partial('/catalogmanager/partial/add.phtml', array('class' => 'option', 'search' => 1, 'add' => 1));?>
                    <?=$this->render('/catalogmanager/partial/product/option.phtml'); // hidden empty option?>
                    <? if (count($product->getOptions()) > 0) : ?>
                        <? foreach ($product->getOptions() as $option) : ?>
                            <?=$this->partial('/catalogmanager/partial/product/option.phtml', array(
                                'option' => $option, 'forms' => $forms,
                            ));?>
                        <? endforeach; ?>
                    <? endif; ?>
                </div>
                <!-- "This Product > Options" Pane -->
                
                <!-- "This Product > Specs" Pane -->
                <div class="tab-pane" id="specs">
                    <div class="add-new">
                        add a spec
                    </div>
                </div>
                <!-- "This Product > Specs" Pane -->

            </div>
            <!-- /panes -->

    </div>
    <!-- /"This Product" Pane -->

    <!-- "Parent Choices" Pane -->
    <div class="tab-pane" id="parent-choices">
            <? if (count($product->getParentChoices()) > 0) : ?>
                <?=$this->partial('/catalogmanager/partial/entity-list.phtml', array('paginator' => $parentChoicesPaginator)); ?>       
            <? endif; ?>  
    </div>
    <!-- /"Parent Choices" Pane -->

</div>
<!-- /panes -->

