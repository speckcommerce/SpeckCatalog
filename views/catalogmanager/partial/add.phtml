<!-- add-import -->
<? if(isset($function)) : ?>
<? 
    /**
     * import = go get the actual entity
     * import-from = The returned value from the form element will assist in creating a NEW 'whatever'  
     */
?>

<? if(!isset($name)){$name = 'passed name';} ?>

    <? if($function === 'add-button') : ?>
        <div class="span1 add-element"><input type="button" class="addButton btn secondary" value="+"/></div>
    <? endif; ?>

    <? if($function === 'import-search') : ?>
        <input class="span3 add-element" type="text" placeholder="import <?=$name;?>"></input>
    <? endif; ?>

    <? if($function === 'import-dropdown') : ?>
        <div class="span3 add-element">
            <select class="span3">
                <option class="disabled">import <?=$name;?></option>
                <? foreach($data as $key => $val) : ?><option value="<?=$key;?>"><?=$val;?></option><? endforeach; ?>
            </select>
        </div>
    <? endif; ?>

    <? if($function === 'import-from-search') : ?>
        <input class="span3 add-element" type="text" placeholder="import from <?=$name;?>"></input>
    <? endif; ?>
    
    <? if($function === 'import-from-dropdown') : ?>
        <div class="span3 add-element">
            <select class="span3">
                <option class="disabled">import from <?=$name;?></option>
                <? foreach($data as $key => $val) : ?><option value="<?=$key;?>"><?=$val;?></option><? endforeach; ?>
            </select>
        </div>
    <? endif; ?>

<? endif; ?>
<!-- /add-import -->
