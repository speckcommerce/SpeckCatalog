<!-- tabs -->
<ul class="tabs" data-tabs="tabs">
    <li class="this-product"><a href="#this-product">This Product</a></li>
    <li class="parent-choices"><a href="#parent-choices">Parent Choices</a></li>
</ul>
<!-- /tabs -->

<!-- panes -->
<div id="my-tab-content" class="tab-content">
    
    <!--"This Product" Pane -->
    <div id="this-product">

            <!-- tabs -->
            <ul class="tabs" data-tabs="tabs">
                <li class="product"><a href="#product">Product</a></li>
                <li class="options"><a href="#options">Options</a></li>
                <li class="specs"><a href="#specs">Specs</a></li>
                <li class="features"><a href="#features">Features</a></li>
                <li class="documents"><a href="#documents">Documents</a></li>
            </ul> 
            <!-- /tabs -->

            <!-- panes -->
            <div id="my-tab-content" class="tab-content">

                <!-- "This Product > Product" Pane -->
                <div class="tab-pane" id="product">
                        <div class="row show-grid product">
                            <? if($product->getType() === 'product') : ?>
                                <div class="span5">
                                    <? $this->form = $this->forms['form']; ?>
                                    <?=$this->render('/catalogmanager/partial/editform.phtml');?>
                                    <? $this->item = $product->getItem(); ?>
                                    <? $this->form = $this->forms['item']['form']; ?>
                                    <?=$this->render('/catalogmanager/partial/editform.phtml');?>
                                </div>  
                                <div class="span11">
                                    <h4>Manufacturer:</h4>
                                    <? if($this->item->getManufacturer()) :?>
                                        <?=$this->item->getManufacturer();?>
                                    <? else : ?>
                                        <? $this->class = 'company'; ?>
                                        <?=$this->render('catalogmanager/partial/add.phtml');?>
                                    <? endif; ?>
                                    <h4>Item Uoms:</h4>
                                    <? if (count($this->item->getUoms()) > 0) : ?>
                                        <? foreach($this->item->getUoms() as $this->itemUom) : ?>
                                            <?=$this->render('/catalogmanager/partial/product/item/item-uom.phtml');?>
                                        <? endforeach; ?>
                                    <? endif; ?>  
                                    <div class="add-new">
                                        <?=$this->render('/catalogmanager/partial/product/item/item-uom.phtml');?>
                                    </div>
                                </div>
                            <? else: ?>
                                <? $this->form = $this->forms['form']; ?>
                                <?=$this->render('/catalogmanager/partial/editform.phtml');?>
                            <? endif; ?> 
                        </div>
                </div>
                <!-- /"This Product > Product" Pane -->

                <!-- "This Product > Options" Pane -->
                <div class="tab-pane" id="options">
                        <? $this->class = 'option'; ?>
                        <?=$this->render('/catalogmanager/partial/add.phtml');?>
                        <? if (count($product->getOptions()) > 0) : ?>
                            <div class="row">
                            <? foreach ($product->getOptions() as $this->option) : ?>
                                <?=$this->render('/catalogmanager/partial/product/option.phtml');?>
                            <? endforeach; ?>
                            </div>
                        <? endif; ?>
                        <div class="add-new">
                                <?=$this->render('/catalogmanager/partial/product/option.phtml');?>
                                <?=$this->render('/catalogmanager/partial/product/option.phtml');?>
                        </div>
                </div>
                <!-- "This Product > Options" Pane -->
                
                <!-- "This Product > Specs" Pane -->
                <div class="tab-pane" id="specs">
                        <div class="add-new">
                            add a spec
                        </div>
                </div>
                <!-- "This Product > Options" Pane -->

            </div>
            <!-- /panes -->

    </div>
    <!-- /"This Product" Pane -->

    <!-- "Parent Choices" Pane -->
    <div class="tab-pane" id="parent-choices">
            <? if (count($product->getParentChoices()) > 0) : ?>
                <? $this->paginator = $parentChoicesPaginator;?>
                <?=$this->render('/catalogmanager/partial/entity-list.phtml'); ?>       
            <? endif; ?>  
    </div>
    <!-- /"Parent Choices" Pane -->

</div>
<!-- /panes -->
