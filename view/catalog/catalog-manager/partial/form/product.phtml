<? $pdir = '/catalog/catalog-manager/partial/'; ?>

<?
$form->prepare();
$form->setAttribute('method', 'post');
$form->setAttribute('id', 'product-' . $product->getProductId());
$form->setAttribute('class', 'live-form');

?>
<?=$this->form()->openTag($form);?>
    <?=$this->formInput($form->get('product_id')); ?>
    <input type="hidden" name="product_type_id" value="<?=$product->getProductTypeId();?>"/>
    <? if(false === $form->isValid()) : ?>
        <? var_dump($form->getData()); ?>
        <div class="alert alert-error">
        <button class="close" data-dismiss="alert" type="button">×</button>
        <strong>Oh snap!</strong>
        Change a few things up and try submitting again.
        </div>
    <? endif; ?>

    <div class="row">
        <div class="span2">
            <? if ($product->has('image')) : ?>
                <img class="img hover_fullsize" src="<?=$this->speckCatalogImage()->product($product->getImage());?>"/>
            <? endif; ?>
        </div>
        <div class="span6">
            <?=$this->formRow($form->get('name')); ?>
            <?=$this->formRow($form->get('description')); ?>
        </div>
        <div class="span3">
            <?=$this->formRow($form->get('manufacturer_id')); ?>
            <? if('2' == $product->getProductTypeId()) : ?>
                <?=$this->formRow($form->get('item_number')); ?>
            <? endif; ?>
        </div>
    </div>

</form>
