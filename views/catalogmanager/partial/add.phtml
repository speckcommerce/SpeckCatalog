
<!-- add-import -->
<? if(isset($function)) : ?>
<? 
    /**
     * import = go get the actual entity
     * import-from = The returned value from the form element will assist in creating a NEW 'whatever'  
     */
?>
<? if(!isset($name)){$name = '';} ?>
    <? if($function === 'add-button') : ?>
        <div class="span2 add-element"><input type="button" class="addButton btn primary" value="+ <?=$name;?>"/></div>
    <? endif; ?>
    <? if($function === 'add-button-ajax') : ?>
        <div class="span2 add-element"><input type="button" class="addButtonAjax btn secondary" value="+ <?=$name;?>"/></div>
    <? endif; ?>
    <? if($function === 'import-search') : ?>
        <div class="span2 add-element"><input type="text" class="span2 import-modal" entityClass="<?=$name;?>" placeholder="import <?=$name;?>"></input></div>
    <? endif; ?>
    <? if($function === 'import-one-search') : ?>
        <div class="span2 add-element"><input type="text" class="span2 import-one-modal" entityClass="<?=$name;?>" placeholder="import <?=$name;?>"></input></div>
    <? endif; ?>
    <? if($function === 'import-dropdown') : ?>
        <div class="span3 add-element">
            <select class="span3">
                <option class="disabled"><?=$name;?></option>
                <? if(isset($data)) : ?>
                    <? foreach($data as $key => $val) : ?><option value="<?=$key;?>"><?=$val;?></option><? endforeach; ?>
                <? endif; ?>
            </select>
        </div>
    <? endif; ?>
<? endif; ?>
<!-- /add-import -->
