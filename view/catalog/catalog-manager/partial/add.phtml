<div class="span1 add-element">
    <form class="add-new">
    <? foreach ($addForm->getElements() as $element) : ?>
        <?=$this->formInput($element); ?>
    <? endforeach; ?>
    </form>
</div>
